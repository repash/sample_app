<% if @feed_items.any? %>
    <ol class="microposts">
      <!-- render 'shared/feed_item' doesnt work wirh collections -->
      <%= render partial: 'shared/feed_item', collection: @feed_items %>
    </ol>
    <%= will_paginate @feed_items %>
<% end %>